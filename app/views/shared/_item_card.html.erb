<div class="item-card">
  <% if item.image_url.present? %>
    <div class="item-card-thumb" style="width: 100%; height: 160px; overflow: hidden; border-bottom: 1px solid #eee;">
      <%= image_tag item.image_url, alt: item.title, style: "width: 100%; height: 100%; object-fit: cover;" %>
    </div>
  <% end %>

  <div class="item-card-content">
    <h3 class="item-card-title"><%= item.title %></h3>

    <p class="item-card-description"><%= truncate(item.description, length: 100) %></p>

    <div class="item-card-footer">
      <span class="item-card-condition"><strong>Condition:</strong> <%= item.condition %></span>
      <% if item.for_sale? %>
        <span class="item-card-tag sale">For Sale</span>
      <% else %>
        <span class="item-card-tag lend">For Lend</span>
      <% end %>
    </div>
  </div>
</div>
